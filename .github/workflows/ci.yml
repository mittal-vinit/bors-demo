name: CI
on:
  push:
    branches: [main, staging, trying]
  pull_request:

jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: build
        run: |
          if [ "${{ github.event_name }}" = "push" ]; then
            echo "poison pill"; exit 1
          fi
          echo ok
